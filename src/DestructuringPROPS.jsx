import React from "react";

export default function DestructuringPROPS({ name, age, city }) {
  return (
    <div>
      <p>name:{name}</p>
      <p>age:{age}</p>
      <p>city:{city}</p>
    </div>
  );
}
